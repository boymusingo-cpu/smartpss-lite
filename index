<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>SmartPSS Lite - Download</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
      background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
      color: #e2e8f0;
      min-height: 100vh;
      line-height: 1.6;
    }

    .container {
      max-width: 1100px;
      margin: 0 auto;
      padding: 0 24px;
    }

    header {
      padding: 80px 0 60px;
      text-align: center;
    }

    h1 {
      font-size: 3.2rem;
      font-weight: 700;
      margin-bottom: 16px;
      background: linear-gradient(90deg, #60a5fa, #a5b4fc);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .subtitle {
      font-size: 1.32rem;
      color: #94a3b8;
      max-width: 600px;
      margin: 0 auto 40px;
    }

    .download-card {
      background: rgba(30, 41, 59, 0.7);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(148, 163, 184, 0.15);
      border-radius: 16px;
      padding: 48px 32px;
      text-align: center;
      max-width: 580px;
      margin: 0 auto 60px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.4);
      transition: transform 0.2s ease, box-shadow 0.3s ease;
    }

    .download-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 30px 60px rgba(0,0,0,0.5);
    }

    .version {
      font-size: 1.1rem;
      color: #94a3b8;
      margin-bottom: 24px;
    }

    .btn-download {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      background: linear-gradient(90deg, #3b82f6, #60a5fa);
      color: white;
      font-size: 1.25rem;
      font-weight: 600;
      padding: 18px 48px;
      border-radius: 12px;
      text-decoration: none;
      transition: all 0.25s ease;
      box-shadow: 0 10px 25px rgba(59,130,246,0.35);
      border: none;
      cursor: pointer;
    }

    .btn-download:hover {
      background: linear-gradient(90deg, #2563eb, #3b82f6);
      transform: translateY(-3px);
      box-shadow: 0 15px 35px rgba(59,130,246,0.5);
    }

    .btn-download:active {
      transform: translateY(1px);
    }

    .file-details {
      margin-top: 28px;
      font-size: 0.95rem;
      color: #94a3b8;
      text-align: left;
      max-width: 420px;
      margin-left: auto;
      margin-right: auto;
      background: rgba(15, 23, 42, 0.4);
      padding: 16px 20px;
      border-radius: 10px;
      border: 1px solid rgba(148,163,184,0.1);
    }

    .file-details strong {
      color: #cbd5e1;
    }

    .file-details code {
      background: rgba(148,163,184,0.2);
      padding: 2px 6px;
      border-radius: 4px;
      font-family: monospace;
      word-break: break-all;
    }

    .vt-badge {
      margin-top: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 12px;
      font-size: 1rem;
      font-weight: 600;
      color: #4ade80;
    }

    .vt-badge img {
      height: 48px;
      width: auto;
      filter: drop-shadow(0 2px 6px rgba(0,0,0,0.4));
      background: transparent;
    }

    .features {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 28px;
      margin: 60px 0;
    }

    .feature {
      background: rgba(15, 23, 42, 0.5);
      border: 1px solid rgba(148,163,184,0.1);
      border-radius: 12px;
      padding: 28px 24px;
      text-align: center;
    }

    .feature h3 {
      color: #60a5fa;
      margin-bottom: 12px;
      font-size: 1.3rem;
    }

    .crafted-by {
      margin-top: 40px;
      font-size: 1rem;
      color: #94a3b8;
      font-style: italic;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
    }

    .crafted-by span {
      background: linear-gradient(90deg, #60a5fa, #c084fc);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      font-weight: 600;
    }

    .heart {
      color: #f472b6;
      font-size: 1.3rem;
    }

    footer {
      text-align: center;
      padding: 60px 0 40px;
      color: #64748b;
      font-size: 0.95rem;
    }

    @media (max-width: 640px) {
      h1 { font-size: 2.6rem; }
      .btn-download { padding: 16px 40px; font-size: 1.15rem; }
    }
  </style>
</head>
<body>

  <div class="container">
    <header>
      <h1>SmartPSS Lite</h1>
      <p class="subtitle">Lightweight edition of Dahua's powerful surveillance management software — Your Custom Build</p>
    </header>

    <div class="download-card">
      <div class="version">Your Custom Version: 1.0 (Personal Build – February 2026)</div>
      
      <a href="https://store-eu-par-2.gofile.io/download/web/ecdd7f3e-ad29-4c6b-bb62-bfeaa24a5128/SmartPS%20lite.zip" 
         class="btn-download" 
         download="SmartPS_lite.zip">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
          <polyline points="7 10 12 15 17 10"/>
          <line x1="12" y1="15" x2="12" y2="3"/>
        </svg>
        Download Your SmartPSS Lite
      </a>

      <div class="file-details">
        <strong>File Details:</strong><br>
        • Size: 78 MB<br>
        • SHA256: <code>replace-with-your-actual-sha256-hash-here (e.g. a1b2c3d4e5f6...)</code><br>
      </div>

      <div class="vt-badge">
        <img src="https://cdn-icons-png.flaticon.com/512/190/190411.png" alt="Safe & Verified">
        Safe & Verified
      </div>

      <div class="crafted-by">
        Crafted with <span class="heart">♥</span> <span>Dahua</span> — Personalized Surveillance Tool
      </div>
    </div>

    <section class="features">
      <div class="feature">
        <h3>Lightweight & Fast</h3>
        <p>Optimized size and performance for smooth operation on everyday hardware</p>
      </div>
      <div class="feature">
        <h3>Full Live View & Playback</h3>
        <p>Multi-channel live monitoring, playback, export, PTZ, and smart search</p>
      </div>
      <div class="feature">
        <h3>Seamless Device Control</h3>
        <p>Easy addition of Dahua & ONVIF devices, batch setup, firmware management</p>
      </div>
    </section>

    <footer>
    </footer>
  </div>

</body>
</html>
